---
import { getCollection } from "astro:content";
// export async function getStaticPaths() {
//   const apps = await fetchApps();

//   return apps.map((app) => ({
//     params: {
//       slug: app.slug,
//     },
//     props: {
//       app,
//     },
//   }));
// }
const result = await getCollection("app");
console.log(result);
---
